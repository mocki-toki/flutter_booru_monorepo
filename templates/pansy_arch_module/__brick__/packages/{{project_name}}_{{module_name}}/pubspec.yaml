name: "{{project_name}}_{{module_name}}"

publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.19.1 <3.0.0"

dependencies:
  "{{project_name}}_core":
    path: ../{{project_name}}_core

flutter:
  uses-material-design: true
