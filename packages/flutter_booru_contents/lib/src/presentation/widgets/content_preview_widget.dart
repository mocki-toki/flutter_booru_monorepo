import 'package:flutter/material.dart';

class ContentPreviewWidget extends StatelessWidget {
  const ContentPreviewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
